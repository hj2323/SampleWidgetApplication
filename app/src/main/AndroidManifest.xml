<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samplewidgetapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleWidgetApplication">
        <activity android:name=".ProgressActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DialogActivity">

        </activity>
        <activity android:name=".ToastActivity"></activity>
        <activity
            android:name=".OrientationActivity2"
            android:configChanges="orientation|screenSize|keyboardHidden" />
        <activity android:name=".OrientationActivity" />
        <activity android:name=".EventActivity" />
        <activity android:name=".SampleDrawableActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>